import { StatusText } from "../../../../components/Status";

export const StatusTab = (props) => {
    const { status: statusData } = props;

    console.log('statusData', statusData);

    return (<div>Status Tab Data</div>);
}
