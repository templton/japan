query unitsByCityId($id: ID!) {
    city(id: $id) {
        title
        phone
        country {
            title
        }
        units{
            id
            address
            coord
        }
    }
}
